# Phase 3, Plan 1: AI Drafting & Billing

**Phase:** 3 — AI Drafting & Billing
**Status:** Complete
**Completed:** 2026-05-04

## Goal

Implement AI-assisted estimate drafting and free/pro tier billing gates so contractors can describe a job and receive a fully-drafted estimate ready to review and edit, with free-tier contractors gated at 5 estimates/month.

## Tasks

### Task 1: Database migration — plan column on organizations
- [x] Add `plan` column (text, default 'free') to `organizations` table via Supabase migration
- [x] Free-tier default; Pro set manually in Supabase dashboard for MVP

### Task 2: `/api/ai/draft-estimate` serverless endpoint
- [x] Create `api/lib/supabase.ts` — auth and service-role Supabase helpers
- [x] Create `api/ai/draft-estimate.ts` Vercel serverless function
- [x] Accepts: `estimate_id`, `description`, `zip_code?`, `qa_pairs?`, `attachment_ids?`, `category?`
- [x] Calls Anthropic API (claude-haiku-4-5-20251001) via `create_estimate_draft` tool
- [x] Returns low/typical/high cents for every line item
- [x] Inserts sections + line items with `source='ai'` tag into Supabase
- [x] Logs to `ai_usage_events` (tokens in/out, cost_cents, model, latency_ms)
- [x] Verifies JWT auth — rejects unauthenticated requests

### Task 3: Billing gate — estimate count check
- [x] Free tier: `SELECT COUNT(*) FROM estimates WHERE organization_id = ? AND created_at >= start_of_month`
- [x] Server-side check in dashboard route / estimate creation
- [x] Hard block at 5/month for free tier; unlimited for pro

### Task 4: Dashboard — usage counter and upgrade modal
- [x] Free-tier dashboard shows "X of 5 estimates used this month" counter
- [x] Counter turns amber at 4/5, red at 5/5
- [x] "New Estimate" at limit → opens upgrade modal instead of creating
- [x] Upgrade modal: mailto CTA to robertchelala@gmail.com
- [x] Pro-tier dashboard: no counter shown

### Task 5: Estimate editor — AI entry point
- [x] Empty estimate body shows "Draft with AI" and "Start manually" options
- [x] AI inline textarea (no modal): "Describe the job..." + Generate button
- [x] Loading state while generating
- [x] AI response populates Zustand store via `addSectionLocal` / `addLineItemLocal`
- [x] Range badge on unit_price cell: $low–high (typical stored in unit_price_cents)
- [x] Range popover: click to select Low / Typical / High value
- [x] "Suggested by AI — review before sending" badge on each AI row
- [x] Badge disappears after contractor picks/edits value; `source='ai'` remains

### Task 6: Vercel deployment fixes
- [x] Wire vercel.json for API routes and SPA rewrite
- [x] Fix TypeScript configuration for API compilation
- [x] Fix env var access at runtime (not module load) for Vercel
- [x] Fix Anthropic model default and endpoint path

## Files Created/Modified

| File | Action |
|------|--------|
| `api/lib/supabase.ts` | Create |
| `api/ai/draft-estimate.ts` | Create |
| `src/services/estimates.ts` | Modify (checkEstimateLimit, createEstimateIfAllowed) |
| `src/stores/editorStore.ts` | Modify (low_cents, typical_cents, high_cents, source fields) |
| `src/components/estimate/EstimateBody.tsx` | Modify (AI entry point, range badge, AI badge) |
| `src/pages/DashboardPage.tsx` | Modify (usage counter, upgrade modal) |
| `supabase/migrations/` | Add plan column migration |
| `vercel.json` | Modify |
| `tsconfig.json` | Modify (API compilation) |
