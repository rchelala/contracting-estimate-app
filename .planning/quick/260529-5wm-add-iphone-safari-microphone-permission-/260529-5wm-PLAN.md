# Quick Task 260529-5wm: Add iPhone Safari microphone permission tip

**Date:** 2026-05-29
**Status:** Executed

## Goal

When microphone access is denied on iOS, show instructions specific to iPhone:
Settings → Apps → Safari → Microphone → Allow

## Task

**File:** `src/hooks/useVoiceInput.ts`
**Action:** Detect iOS via `navigator.userAgent` in the `not-allowed`/`service-not-allowed` error branch.
Return iOS-specific message; fall back to generic message on other platforms.

Both `WizardStep4Describe` and `WizardStep5QA` render the `error` string from this hook,
so both components benefit automatically without additional changes.
